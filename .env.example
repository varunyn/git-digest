# Optional: copy to .env or ~/.config/git-digest/.env
# These override values in repos.yaml. CLI flags override env.

# Ollama (for --ai-summary and MCP use_ai_summary)
# OLLAMA_BASE_URL=http://127.0.0.1:11434
# OLLAMA_MODEL=gemma3n
# OLLAMA_TIMEOUT=120

# App defaults
# GIT_DIGEST_CACHE_DIR=~/.cache/git-digest
# GIT_DIGEST_DEFAULT_TITLE=Git updates summary
